# Solución - Lección 08

1. Prepara Serial con 9600 símbolos por segundo según la configuración usada.
2. `print` continúa en la misma línea; `println` añade final de línea.
3. Está en `setup()`.
4. Suma uno al valor actual y guarda el resultado.
5. Vuelve a 0 porque la variable está en SRAM y se inicializa al arrancar.
6. Permite observar datos y decisiones antes de añadir el riesgo y complejidad del movimiento.
